dokka {

    outputDirectory = "$rootDir/docs"
    outputFormat = "html"

    configuration {
        sourceRoot {
            path = "$rootDir/service/src"
        }
        sourceRoot {
            path = "$rootDir/models/src"
        }
        sourceRoot {
            path = "$rootDir/core/src"
        }
        sourceRoot {
            path = "$rootDir/link/src"
        }
    }
}
